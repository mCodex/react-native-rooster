import React from 'react';
import { TextInput } from 'react-native';

import { Container } from './styles';

const Home: React.FC = () => {
  return (
    <Container>
      <TextInput />
    </Container>
  );
};

export default Home;
